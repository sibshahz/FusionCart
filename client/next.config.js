/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // remotePatterns:['localhost'],
    domains: ['localhost'],
  },
}
// module.exports = {
//   images: {
//     domains: ['localhost'],
//   },
// }
module.exports = nextConfig
